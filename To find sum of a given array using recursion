#include<bits/stdc++.h>
using namespace std;
int sumarray(int arr[],int size){
    if(size==0) return 0;
    else return arr[0]+sumarray(arr+1,size-1);
}
int main(){
    int size=6;
    int arr[size]={1,3,5,7,23,56};
    cout<<sumarray(arr,size);
}
